{
  "name": "@velite/benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/init.js",
    "dev": "velite dev --verbose",
    "build": "velite build --verbose"
  },
  "dependencies": {
    "velite": "latest"
  }
}
